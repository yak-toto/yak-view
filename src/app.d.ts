declare global {
    namespace App {
        // interface Error {}
        interface Locals {
            accessToken?: string;
        }
        // interface PageData {}
        // interface PageState {}
        // interface Platform {}
    }
}

export {};
